MODULES_GUIDE.md is a documentation file that should not be added to .gitignore. The change list contains only a source/config file (.md) and no build artifacts, dependencies, or temp files.

(empty — output nothing)